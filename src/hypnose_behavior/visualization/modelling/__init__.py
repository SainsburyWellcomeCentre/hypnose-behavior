"""Figures for the behavioural sequence models in ``hypnose_behavior.modelling``.

One subpackage per analysis, mirroring ``hypnose_behavior.modelling``. Figures depend on the model
results (and on ``hypnose_behavior.modelling`` for the fitted curves); the model code never depends on
these, so the dependency runs one way, visualization -> modelling.
"""
