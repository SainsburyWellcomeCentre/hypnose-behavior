"""Quality-control tooling: data validation and golden-master regression.

Run the regression/byte-comparison tools after major changes to confirm the
pipeline output is unchanged (or to mark what changed, if intended).
"""
