"""Movement metrics derived from SLEAP tracking.

Docstring-only, like every package `__init__.py` in this repo: importing a
submodule must trigger no package-level side effects (`docs/DECISIONS.md`
section 3). There is therefore **no re-export** -- import
`hypnose_behavior.metric_analysis.movement.speed_analysis` by name.
"""
